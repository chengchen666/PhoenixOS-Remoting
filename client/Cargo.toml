[package]
name = "client"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
device_buffer = { path = "../device_buffer" }

[lib]
crate-type = ["cdylib"]
