[package]
name = "device_buffer"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
libc = "*"
num = "0.2"
num-traits = "0.2"
num-derive = "0.4"
log = "0.4.0"
